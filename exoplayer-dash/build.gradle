// exoplayer-dash/build.gradle

apply plugin: 'maven-publish'

publishing {
    publications {
        aar(MavenPublication) {
            groupId = 'com.github.phuongwd'
            artifactId = 'exoplayer-dash'
            version = '2.10.1'

            artifact file('libs/exoplayer-dash-2.10.1.aar')
        }
    }
}
